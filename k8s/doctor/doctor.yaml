apiVersion: v1
kind: Service
metadata:
  name: doctor-service
spec:
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: doctor-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: doctor-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: doctor-service
  template:
    metadata:
      labels:
        app: doctor-service
    spec:
      containers:
        - image: medinsight/doctor-service:latest
          name: doctor-service
          imagePullPolicy: IfNotPresent
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: k8s
            - name: DB_URL
              value: jdbc:postgresql://postgres:5432/medinsight_db
            - name: KEYCLOAK_URL
              value: http://keycloak:8080
          ports:
            - containerPort: 8080
